PROJECT_NAME="Where's My Money API"
API_V1_STR="/api/v1"

# Database Configuration (PostgreSQL default, falls back to SQLite if PostgreSQL connection fails)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_SERVER=localhost
POSTGRES_PORT=5432
POSTGRES_DB=whereismymoney
